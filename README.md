# Thought of Journey

Frontend-only hackathon project using the three supplied datasets.

## Run
1. Open this folder in VS Code.
2. Open terminal.
3. Run `npm install`
4. Run `npm run dev`
5. Open the localhost URL shown by Vite.

Datasets are already included in `public/data/`.
